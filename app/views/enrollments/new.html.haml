-#Отображение названия статьи в заголовке страницы
- content_for :title do
  University
  \|
  New enrollment

.card
  .card-body
    %h3
      .fa.fa-graduation-cap
      = link_to @course.title, course_path(@course)
      %hr
      .fa.fa-lock-open
      Buy course:

  .card-body
    - if @course.price > 0
      = form_tag course_enrollments_path(@course) do
        -#Платно
        %script.stripe-button{ "data-amount" => "#{(@course.price * 100).to_i}",
                              "data-description" => "#{@course.title} (#{number_to_currency @course.price})",
                              "data-email" => "#{current_user.email}",
                              "data-key" => "pk_test_51H6ZGHHtJ9XMkl4Hy70xbacwBBa90sxij3MMOXjiEQi1upTtG2lLw8IWcOw0QjO9DEZLGkBtLFmtT96vJwwKVOEb00FFw7rpiY",
                              "data-locale" => "auto",
                              :src => "https://checkout.stripe.com/checkout.js"}
    - else
      / Бесплатно
      = form_tag course_enrollments_path(@course) do
        = submit_tag "Free", class: 'btn btn-lg btn-success'

  .card-footer
    = link_to 'Back', course_path(@course)
